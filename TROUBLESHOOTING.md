# TROUBLESHOOTING — SalaryCast_AI (StoveLens AI)

문제를 해결하고 검증까지 끝낸 직후에 누적한다. 최신 항목이 위로 온다.

## 인덱스

| 번호 | 제목 | 날짜 | 분류 |
|---|---|---|---|
| TS-010 | 국가대표 명단을 대회 문서에서 읽어 아시안게임은 0명, 올림픽은 과다 집계 | 2026-08-17 | 데이터 / 외부 API |
| TS-004 | 스타성 데이터가 손타이핑 창작값이었고, 그게 성능을 떠받치고 있었음 | 2026-08-16 | 데이터 / 모델 |
| TS-003 | 부상 시즌을 빼자 예측이 나빠짐 — 규칙과 측정이 어긋난 건 | 2026-08-16 | 모델 |
| TS-002 | FA 계약 원본의 생년이 절반 이상 틀림 → 나이 피처 오염 | 2026-08-16 | 데이터 |
| TS-001 | 네이버 KBO API 파라미터 변경 + 시즌당 선수 70% 누락 | 2026-08-16 | 외부 API / 데이터 수집 |
| TS-009 | 선수 사진이 깨진 아이콘 — CDN이 Referer로 도용 차단 | 2026-08-03 | 외부 API / UI |
| TS-008 | 컬럼 `overflow: hidden` 때문에 호버 팝업이 잘림 | 2026-08-03 | UI |
| TS-007 | 소스를 고쳐도 브라우저 화면이 그대로 — 재실행 배너가 CSS에 숨겨짐 | 2026-08-03 | 환경 / UI |
| TS-006 | 카드 HTML이 들여쓰기 때문에 코드블록으로 그대로 노출됨 | 2026-08-03 | UI |
| TS-005 | Streamlit 카드가 전부 빈 회색 박스 — HTML 태그를 여러 호출에 쪼갬 | 2026-08-03 | UI |

> TS-005~009는 `docs/TROUBLESHOOTING.md`에 따로 적어두던 UI 작업 기록을 이 파일로 합친 것이다.
> 번호는 이관 시점에 붙였고 날짜는 원 기록 기준이라, 인덱스에서는 TS-001~004보다 아래에 온다.

---

## 기록 템플릿

```markdown
## TS-NNN — 제목

- **날짜**: YYYY-MM-DD
- **분류**: (빌드 / 배포 / 데이터 / 외부 API / 의존성 / 모델 / UI)

### 증상
관측된 현상. 에러 로그는 원문 그대로.

### 재현 조건
언제, 어떤 입력·환경에서 발생하는가.

### 원인
- **표면 원인**: 직접적으로 터진 지점
- **근본 원인**: 왜 그 지점이 터지게 됐는가
- 확인한 것과 추측한 것을 구분해서 적는다.

### 시도했지만 안 된 것
실패한 접근과 왜 안 됐는지.

### 해결
실제로 적용한 조치.

### 검증
어떻게 고쳐진 걸 확인했는가. 명령어와 출력.

### 추후 관리
재발 방지, 모니터링 포인트.

### 배운 점
```

---

## 기록

## TS-001 — 네이버 KBO API 파라미터 변경 + 시즌당 선수 70% 누락

- **날짜**: 2026-08-16
- **분류**: 외부 API / 데이터 수집

### 증상

두 개가 겹쳐 있었다.

**(1) 앱에서 홍창기의 2025 시즌 기록이 조회되지 않음.**
`data/hitter_season_stats_2015_2026_v2.csv` 기준 홍창기의 시즌이 2020·2021·2022·2023·2024·2026만 존재하고 2025가 비어 있었다.

```
 collect_year teamName  hitterGameCount  hitterAb  hitterOps
         2022       LG              118       437      0.745
         2023       LG              141       524      0.856
         2024       LG              139       524      0.857
         2026       LG               60       213      0.688     <- 2025 없음
```

**(2) 기존 크롤 스크립트를 재실행하면 전건 실패.**

```
{"code":400,"message":"invalid request : field, direction","errorType":"INVALID_PARAMETER","success":false}
```

### 재현 조건

- (1) `data/*_season_stats_2015_2026_v2.csv`를 읽어 특정 선수의 `collect_year`를 나열하면 중간 연도가 비어 있다. 홍창기 외 다수.
- (2) `scripts/crawl_extend.py`를 실행하면 모든 연도·정렬기준에서 400. 2026-08-16 기준 재현율 100%.

### 원인

**표면 원인 (1)**: 시즌 스탯 CSV에 해당 연도 행이 없음.

**근본 원인 (1) — 확인함**: 부상 결장이 아니라 **수집 누락**이었다. 네이버 API를 직접 조회하니 2025 홍창기 기록이 정상 존재한다.

```
2025 홍창기: 51경기, 타수 174, OPS .727, WAR 1.23
```

기존 수집은 정렬 기준별 상위 N명만 가져오는 구조에 `pageSize=100`이 걸려 있어, 부상 등으로 출전이 적은 선수가 어떤 정렬 기준에서도 상위권에 못 들면 통째로 빠졌다. 누락 규모는 국지적이 아니라 전면적이었다.

| 연도 | API 실제 타자 수 | 수집 보유 | 누락률 |
|---|---|---|---|
| 2024 | 386 | 105 | 73% |
| 2025 | 400 | 119 | 70% |
| 2026 | 332 | 107 | 68% |

**표면 원인 (2)**: HTTP 400 `invalid request : field, direction`

**근본 원인 (2) — 확인함**: 네이버 API가 정렬 파라미터 이름을 바꿨다. `sortField` + `sortDirection` → `field` + `direction`. 에러 메시지가 "field, direction이 invalid"라고 말하지만 실제로는 **그 이름의 파라미터가 아예 오지 않아서** 나는 메시지였다. 보내지도 않은 파라미터 이름을 지적하는 형태라 원인이 직관적으로 안 이어졌다.

```
sortField=hitterWar&sortDirection=DESC  -> 400
field=hitterWar&direction=DESC          -> 200
```

### 시도했지만 안 된 것

- **처음 세운 가설: "홍창기가 2025년 부상으로 규정 타석을 못 채워 기록이 없다"** — 사용자도 같은 인식이었고 나도 그대로 받아들였다. 틀렸다. 규정 타석 미달과 기록 부재는 별개다. 네이버 API는 1경기만 뛰어도 행을 준다. 원본을 직접 확인하고 나서야 수집 문제라는 게 드러났다.
- **`sortDirection`만 `direction`으로 바꿔봄** — 여전히 400. `sortField`도 같이 `field`로 바꿔야 통과했다.
- **CSV의 `playerImageUrl` 컬럼을 선수 사진으로 쓰려 함** — 전량 `https://sports-phinf.pstatic.net/player/kbo/default/{id}.png` 형태이고 **전부 HTTP 404**였다. 1,320건 중 유효 0건.
  ```
  curl -o /dev/null -w "%{http_code}" ".../player/kbo/default/71207.png"  ->  404
  ```
  실제 사진은 응답의 `profile` 컬럼(JSON 문자열) 안 `image` 키에 들어 있었다.

### 해결

`scripts/crawl_naver_v2.py`를 새로 작성했다. `crawl_extend.py`는 2013~2016 보충 병합 전용이고 제출본에 사본이 있어 수정하지 않고 남겨뒀다.

바꾼 것 세 가지:

1. **파라미터**: `field` + `direction=DESC`
2. **`pageSize=500` + 페이지 순회**. 반환 건수가 `PAGE_SIZE` 미만이면 종료. 정렬 기준도 출전 기반을 추가해 하위권 선수를 덮었다.
   - 타자: `hitterWar`, `hitterGameCount`, `hitterAb`
   - 투수: `pitcherWar`, `pitcherGameCount`, `pitcherSave`, `pitcherHold`
3. **사진은 `profile` JSON에서 파싱**. `playerImageUrl`은 버리고 `json.loads(profile)["image"]`를 쓴다.

출력 (기존 파일 덮어쓰지 않음):
- `data/naver_hitter_2013_2026_raw_v2.csv`
- `data/naver_pitcher_2013_2026_raw_v2.csv`
- `data/player_photos.csv`

### 검증

**수집량** — 타자 604명 → 1,273명, 투수 794명 → 954명.

```
[1/2] 타자
  [2024]  386명   [2025]  398명   [2026]  332명
[2/2] 투수
  [2024]  291명   [2025]  281명   [2026]  281명

저장: data/naver_hitter_2013_2026_raw_v2.csv  (5003행, 1273명)
저장: data/naver_pitcher_2013_2026_raw_v2.csv  (3637행, 954명)
저장: data/player_photos.csv  (1566명)
```

**홍창기 2025 복구 확인** — 누락됐던 2025가 들어왔고, 2026도 60경기 시점 스냅샷에서 97경기로 갱신됐다.

```
 collect_year  hitterGameCount  hitterAb  hitterOps  hitterWar
         2024              139       524      0.857       4.79
         2025               51       174      0.727       1.23   <- 복구
         2026               97       338      0.674       1.24   <- 60경기에서 갱신
```

**사진 유효성** — `default/` 형태 404 URL 0건. 실제 응답 확인:

```
구자욱    HTTP 200  68218B
김도영    HTTP 200  61234B
박동원    HTTP 200  64181B
최정     HTTP 200  65079B
홍창기    HTTP 200  65195B
```

**사진 커버리지** — 현역 기준 사실상 전원.

```
2026 시즌 기록 보유 541명 / 사진 없음 2명 (0.4%)  -> 사토시, 이서준
2025 시즌 기록 보유 564명 / 사진 없음 1명 (0.2%)  -> 최상민
전체(2013~2026) 1,732명 / 사진 보유 1,566명 (90.4%)
```

결측 166명은 대부분 과거 은퇴 선수(강정호, 강봉규, 고메즈 등)다.

### 추후 관리

- **네이버 API는 사전 공지 없이 파라미터가 바뀐다.** 수집 스크립트에 `payload.get("success")` 체크를 넣어 조용히 빈 결과로 넘어가지 않게 했다. 실패 시 `[실패] {year} {type} {field}: {message}` 출력.
- **2026은 진행 중 시즌**이다(최대 97경기 / 144경기, 약 67%). 시즌 종료 후 재수집 필요. 누적 스탯(홈런·타점·WAR·이닝·세이브·홀드)을 그대로 쓰면 과소 평가되므로 시즌 환산 처리가 별도로 필요하다.
- 수집 시점이 값에 직접 영향을 주므로, 앞으로 재수집할 때는 `player_photos.csv`의 `collected_at`뿐 아니라 원시 CSV에도 수집 시각을 남기는 편이 낫다.
- 사진 결측 3명(사토시·이서준·최상민)은 수동 보완 대상.

### 배운 점

- **"기록이 없다"를 그대로 믿으면 안 된다.** 우리 데이터에 없는 것과 원본에 없는 것은 다르다. 부상이라는 그럴듯한 설명이 붙어 있으면 더 의심해야 한다. 원본을 한 번 찔러보는 비용이 훨씬 싸다.
- **외부 API 400 메시지는 "무엇이 틀렸다"가 아니라 "무엇이 필요하다"일 수 있다.** `invalid request : field, direction`은 보낸 값이 틀렸다는 뜻이 아니라 그 이름의 파라미터가 없다는 뜻이었다.
- **모델 정확도 개선이라고 해서 항상 모델 문제인 건 아니다.** 이번 건은 하이퍼파라미터가 아니라 학습 표본이 실제의 30%였던 문제다. 데이터 커버리지를 먼저 확인해야 했다.
- 응답에 이미 들어 있는 필드(`profile`)를 못 보고, 선수마다 API를 다시 호출하는 코드를 앱에 넣어뒀었다. 응답 원본을 한 번 통째로 들여다보는 게 먼저다.

---

## TS-002 — FA 계약 원본의 생년이 절반 이상 틀림 → 나이 피처 오염

- **날짜**: 2026-08-16
- **분류**: 데이터

### 증상

네이버 스포츠 API는 생년월일을 주지 않는다. `data/fa_contracts_v3.csv`에만
`birth_year`가 있어 그걸 정답으로 삼고, 없는 선수는 결측으로 두고 있었다.

빈 생년을 채우려고 위키데이터에서 KBO 선수 3,295명의 생년을 받아
검증 삼아 v3의 140건과 대조했더니 **107건 중 52건이 어긋났다.**

```
장민재  v3=1998  위키데이터=1990   (8년)
장시환  v3=1993  위키데이터=1987   (6년)
홍건희  v3=1998  위키데이터=1992   (6년)
허경민  v3=1993  위키데이터=1990   (3년)
```

### 재현 조건

`data/fa_contracts_v3.csv`의 `birth_year`를 쓰는 모든 경로. 상시.

### 원인

- **표면 원인**: v3의 `birth_year` 컬럼 값이 틀림.
- **근본 원인**: v3은 수기로 만든 파일이고, `age_at_fa`가 `fa_year - birth_year`로
  파생된다(140건 중 139건 일치 확인). 즉 **학습 데이터의 나이가 54건 틀린 채로
  모델에 들어가고 있었다.** 나이는 상위 기여 피처라 영향이 직접적이다.
- 어느 쪽이 맞는지는 추측하지 않고, 생년이 널리 알려진 선수로 교차 확인했다.
  허경민 1990 · 정수빈 1990 · 전준우 1986 · 정우람 1985 · 장민재 1990 ·
  장시환 1987 · 홍건희 1992 · 김강률 1988 — 전부 위키데이터 쪽이 맞았다.

### 시도했지만 안 된 것

1. **첫 시즌으로 생년 추정** — 데뷔 나이를 18~23 중 어느 값으로 잡아도
   평균 오차 5.5~8년. 데이터가 2013년부터라 그 이전 데뷔 선수는 첫 시즌이
   실제 데뷔가 아니고, 외국인 선수는 KBO 첫 시즌이 20대 후반이다.
   `isKoreanPlayer` 필드로 국적을 가르려 했으나 8,640 레코드 전부 False였다.
   6년 오차면 `prime_years_left`가 뒤집히므로 폐기하고 결측으로 뒀다.
2. **한국어 위키백과 검색 API** — 선수마다 1회 호출하니 5명째에 HTTP 429.
   제목을 추정해 50개씩 묶어도 429.

### 해결

위키데이터 SPARQL(`query.wikidata.org/sparql`) 질의 2번으로 3,295명 확보.
`scripts/collect_player_birth.py`, `scripts/fix_fa_birth_year.py`.

- `data/fa_contracts_v4.csv` — 54건 교정, `birth_year_source` 컬럼(`원본`/`wikidata`) 추가.
  v3은 덮어쓰지 않고 남겼다.
- `data/player_birth_manual.csv` — 결측이던 420명 확보. **이름이 아니라 `player_id`가 키**다.
  같은 이름이 96쌍이라 이름으로 붙이면 다른 사람의 생년이 들어온다.
- 미해결 229명(위키데이터 문서 없는 저연차·외국인 205, 동명이인 구분 실패 24)은
  나이를 결측으로 둔다. 추정치를 넣는 것보다 낫다.

### 검증

`output/reports/birth_year_corrections.md` — 54건 교정 / 86건 유지, 평균 2.3년, 최대 8년.
교정 후 재학습에서 투수 R² 0.573 -> 0.720.

### 추후 관리

- 새 FA 계약을 추가할 때 `birth_year`를 손으로 넣지 말고
  `scripts/collect_player_birth.py`로 대조할 것.
- 미해결 229명은 화면에 "나이 정보 없음"으로 표시된다.

### 배운 점

**가진 데이터를 정답으로 두고 외부 데이터를 검증하려다, 반대로 가진 데이터가
틀린 걸 찾았다.** 채우려던 컬럼을 검증에 써본 게 결정적이었다.

---

## TS-003 — 부상 시즌을 빼자 예측이 나빠짐 (규칙과 측정이 어긋난 건)

- **날짜**: 2026-08-16
- **분류**: 모델

### 증상

"부상으로 시즌을 못 채운 해는 건너뛰고 더 과거로 내려가라"는 요구를 구현했다.
홍창기 2025년(51경기 174타수)을 빼고 2026·2024·2023을 쓰게 하는 규칙이다.

의도대로 동작했지만(학습 139건 중 26건의 시즌 구성이 바뀜, 강백호 2022·2023,
함덕주 2021·2022, 심우준 2024 등 실제 부상 시즌을 정확히 제외) **성능이 떨어졌다.**

```
투수 R²  0.720 -> 0.640
타자 R²  0.528 -> 0.520
```

### 재현 조건

`src/features.py`의 `select_recent_seasons`에 출전량 하한(타자 72경기 / 투수 30이닝)을
걸고 `scripts/build_training_v8.py` -> `scripts/train_model_v8.py` 실행.

### 원인

- **표면 원인**: 짧은 시즌을 빼면 3시즌 평균 성적이 좋아지는데, 정답(실제 계약금)은
  그대로라 예측이 위로 치우친다.
- **근본 원인**: **결장 자체가 계약금에 반영되는 정보였다.** 부상으로 한 해를 날린
  선수는 시장에서 실제로 깎여서 계약한다. 모델은 그 신호를 깎인 누적 스탯으로
  학습하고 있었는데, 시즌을 빼면서 그 신호가 통째로 사라졌다.

### 시도했지만 안 된 것

**사라진 신호를 피처로 되돌리기.** 고른 세 시즌의 연도 폭(`season_span`,
연속이면 2·한 해 건너뛰면 3 이상)을 추가했다. 표본이 46~93건이라 노이즈였다.

```
타자 0.541 -> 0.540   투수 0.718 -> 0.718
```

### 해결

규칙을 되돌리고, 원래 의도(짧은 시즌이 평균을 왜곡하는 것)는 다른 방식으로 잡았다.

**비율 스탯을 출전량 가중평균으로 계산한다.** 이전에는 시즌별 단순 평균이라
174타수 시즌의 OPS가 524타수 시즌과 같은 무게를 가졌다. 타수(투수는 이닝)로
가중하면 짧은 시즌의 영향이 표본 크기만큼만 반영된다.
(`src/features.py` `_weighted_mean`)

4가지 조합 전수 비교 (억 원 OOF, 5-fold x 5회, 블렌드 탐색 포함):

| 부진시즌 제외 | 출전량 가중 | 타자 R² | 투수 R² |
|---|---|---|---|
| X | X | 0.528 | 0.720 |
| **X** | **O** | **0.541** | **0.718** |
| O | X | 0.520 | 0.640 |
| O | O | 0.537 | 0.636 |

가중은 항상 이득(타자 +0.013~0.017), 제외는 항상 손해(투수 -0.08).

### 검증

`pytest tests/test_features.py` — `test_aggregate_seasons_weights_rate_stats_by_playing_time`가
단순 평균과 가중 평균이 갈리는 것을 고정한다.

홍창기 2024~2026 OPS: 단순 평균 .753 -> 타수 가중 .776.

### 추후 관리

FA 계약 표본이 200건을 넘으면 `season_span` 같은 결장 피처를 다시 볼 만하다.
지금은 46~93건이라 피처를 하나 더 넣는 비용이 이득보다 크다.

### 배운 점

**"이렇게 하는 게 맞다"는 규칙과 "이렇게 하면 더 잘 맞는다"는 측정이 어긋날 때,
규칙이 겨냥한 게 뭔지 다시 봐야 한다.** 여기서 요구의 핵심은 '부상 시즌을 빼라'가
아니라 '짧은 시즌이 평가를 망치지 않게 하라'였고, 그건 제외가 아니라 가중으로
푸는 문제였다. 구현하고 재보지 않았으면 성능이 떨어진 채로 넘어갔을 것이다.

---

## TS-004 — 스타성 데이터가 손타이핑 창작값이었고, 그게 성능을 떠받치고 있었음

- **날짜**: 2026-08-16
- **분류**: 데이터 / 모델

### 증상

미래 FA 후보 42명 중 33명이 스타성 0점으로 처리되고 있었다. 홍창기·구자욱·
원태인처럼 골든글러브·국가대표 경력이 있는 선수도 '수상 경력 없음'이었다.

원인을 보니 `data/star_features_hitter.csv` / `star_features_pitcher.csv`가
`scripts/make_star_features.py`에 **파이썬 dict로 직접 타이핑된 상수**였다.
출처가 없고, FA 계약자 140명분만 있었다.

### 재현 조건

FA 계약 이력이 없는 선수를 검색하면 상시. 스타성 관련 피처가 전부 0으로 들어간다.

### 원인

- **표면 원인**: 스타성 파일에 140명분만 있음.
- **근본 원인**: 데이터를 수집한 게 아니라 사람이 기억으로 적었다.
  검증해 보니 값 자체가 틀린 것이 많았다.

| 선수 | 항목 | 손타이핑 | 실제 |
|---|---|---|---|
| 양의지 | MVP | 1 | 0 (정규시즌 MVP 수상 없음) |
| 손아섭 | MVP | 1 | 0 |
| 강백호 | MVP | 1 | 0 |
| 박병호 | MVP | 1 | 2 (2012·2013) |
| 오승환 | 골든글러브 | 7 | 0 |
| 정우람 | 골든글러브 | 4 | 0 |
| 민병헌 | 골든글러브 | 1 | 0 |
| 손아섭(2018) | 골든글러브 | 2 | 5 |

투수 쪽이 특히 심했다. KBO 골든글러브는 **투수 자리가 한 해 한 명뿐**인데
불펜 투수들에게 4~7회가 적혀 있었다. 타자 80건 중 51건, 투수 43건 중
대부분이 실제와 달랐다.

### 시도했지만 안 된 것

1. **위키데이터** — KBO 골든글러브 수상자가 4명뿐이라 못 썼다.
2. **올스타 선정 횟수 복원** — 한국어 위키백과에 2015~2024 중 9개 연도만 있고
   2014년 이전이 전무하다. 있는 연도도 표에 감독·코치가 섞여 있다. 일부 시기만
   세면 옛 선수가 체계적으로 과소 집계되므로 항목 자체를 뺐다.
3. **아시안게임 국가대표 명단** — 한국어 위키백과에 야구 종목 선수 명단 문서가
   없다. 2014·2018·2022 세 대회가 빠졌다.
4. **경력 길이로 대체** — 아래 '해결'의 성능 하락을 메우려고 FA 이전 누적
   시즌 수·WAR·경기 수를 넣어 봤다. 오히려 떨어졌다. 우리 시즌 데이터가
   2013년부터라 그 이전 경력이 잘려 있어, 옛 FA일수록 경력이 짧아 보인다.

```
타자 0.538 -> 0.527 (경력 3종)  /  0.521 (시즌 수만)
투수 0.649 -> 0.629 (경력 3종)  /  0.641 (시즌 수만)
```

### 해결

연도별 수상자 명단 원본에서 다시 만들었다.

- `scripts/collect_star_features.py` — 한국어 위키백과에서 수집
  - 골든글러브 440건 (1982~2025, 매년 10명 전량)
  - MVP 39개 연도
  - 국제대회 10개 (2006·2013·2017·2023 WBC, 2008·2021 올림픽,
    2015·2019 프리미어12, 2017·2023 APBC)
- `scripts/build_star_features.py` — 이름을 `player_id`에 연결
  - MVP·골든글러브는 선수 전용 명단이라 경력 범위 안이면 연결
  - 국가대표는 명단에 감독·코치가 섞여 있어 **대회 전후 2년 안에 출전 기록이
    있는 경우에만** 연결. 은퇴 후 코치로 이름이 오른 옛 선수를 거른다
- 출력 `data/star_features_v2.csv` — 173명 427건. FA 계약자뿐 아니라 전 선수.

`star_score = MVP×5 + 골든글러브×3 + 국가대표×2`

### 검증

교차 확인한 값이 전부 맞았다. 최정 8회·양의지 10회·최형우 8회·김현수 5회
골든글러브, 박병호 MVP 2회(2012·2013), 김광현·양현종 골든글러브 각 1회.

2016년 골든글러브 외야수 원문이 김주찬·최형우·김재환이고 민병헌은 표 전체에
한 번도 나오지 않는다. 손타이핑 값(민병헌 1회)이 틀린 것이 확정됐다.

### 성능 — 고쳤더니 떨어졌다

```
                    타자 R²    투수 R²
손타이핑 스타성          0.541     0.718
수집 스타성            0.538     0.649
```

투수가 크게 떨어져 원인을 쪼개 봤더니, 손타이핑 항목 중 `allstar_count`
하나만 되돌려도 회복됐다.

```
수집 스타성 + 옛 allstar_count   타자 0.524   투수 0.730
```

**그런데 이 값은 쓰면 안 된다.** 두 가지 이유다.

1. 출처가 없고 FA 계약자 140명분만 존재한다. 화면에서 아무 선수나 검색하면
   0이 들어간다. 학습 때는 거물에게 5~10이 붙어 있다가 서빙에서는 전원 0이
   되므로, **지금 고치려던 '미래 FA 후보가 체계적으로 낮게 나오는 문제'를
   그대로 되살린다.**
2. 값 자체가 사람이 '누가 거물인지 이미 알고' 적은 숫자다. 정답과 상관되는 게
   당연하고, 이건 성능이 아니라 후견지명이다.

대신 검증 가능한 신호를 찾았다. 국가대표를 0/1이 아니라 **선발 횟수**로 세니
둘 다 올랐다.

```
국가대표 0/1      타자 0.538   투수 0.649
국가대표 횟수      타자 0.545   투수 0.679
```

### 추후 관리

- 아시안게임 3개 대회(2014·2018·2022)와 2024 프리미어12·2026 WBC 명단은
  미수집. 김형준·나균안처럼 아시안게임 대표 경력만 있는 선수가 0으로 남는다.
- MVP는 1988·1999·2001~2003년이 빠졌다. 이름 칸이 rowspan으로 묶인 행에서
  첫 링크가 소속팀이라 팀명이 잡히는 것을 막으면서 같이 떨어져 나갔다.
  해당 연도 수상자는 김성한·이승엽으로, 둘 다 우리 FA 표본에 없어 영향은 없다.
- 새 시즌 수상자가 나오면 `collect_star_features.py`를 다시 돌리면 된다.
  손으로 값을 적어 넣지 말 것.

### 배운 점

**지표가 떨어졌다고 되돌리면 안 되는 경우가 있다.** 옛 값이 더 잘 맞은 이유는
그게 더 정확해서가 아니라, 사람이 결과를 알고 적은 숫자였기 때문이다.
서빙에서 못 쓰는 피처로 올린 점수는 실제 사용자가 보는 예측을 오히려 나쁘게
만든다. 학습 표본에만 있는 피처를 볼 때는 '이 값을 실제 서비스에서 구할 수
있는가'를 먼저 물어야 한다.

---

## TS-005 — Streamlit 카드가 전부 빈 회색 박스 (HTML 태그를 여러 호출에 쪼갬)

- **날짜**: 2026-08-03
- **분류**: UI

### 증상

홈 화면의 사용 방법·서비스 안내·다음 FA 주목 선수 카드, 검색 화면의 선수 정보
카드·연봉 카드·구단별 제시가 카드가 전부 배경 스타일만 두른 빈 박스로 뜨고,
안에 들어가야 할 텍스트와 수치는 그 카드 **바로 아래에** 스타일 없는 맨 텍스트로
떨어져 나왔다. 파이썬 쪽에서는 아무 오류도 안 났고 콘솔에도 흔적이 없었다.

### 재현 조건

`src/ui/pages.py`의 `render_home`, `render_search`에서 카드를 그릴 때
여는 태그 한 줄을 `st.markdown`으로 먼저 찍고 → 내용을 여러 번의 `st.markdown`으로
채운 뒤 → 마지막에 또 다른 `st.markdown`으로 닫는 태그를 찍는 패턴.
이 패턴을 쓴 카드는 예외 없이 항상 재현됐다.

### 원인

- **표면 원인**: 짝이 안 맞는 `<div>`가 브라우저에서 즉시 닫힌다.
- **근본 원인 — 확인함**: `st.markdown` 호출 하나하나가 **독립된 DOM 엘리먼트**로
  삽입된다. 여러 호출로 쪼갠 HTML 조각들은 브라우저에서 하나로 이어진 문서가
  아니라 서로 남남인 별도 블록이다. 여는 태그만 담긴 조각을 만나면 브라우저가
  그 자리에서 태그를 닫아버리므로, 그 div는 배경만 남은 빈 박스가 되고 진짜
  내용은 스타일 없는 다른 엘리먼트로 떨어져 나온다.

### 시도했지만 안 된 것

- **전역 CSS의 컬럼 `overflow: hidden` 규칙이 원인이라고 추측함** — 규칙이 실제로
  있긴 했지만 그건 이 빈 박스를 **가리려고** 넣어둔 임시방편이었지 원인이 아니었다.
  없애도 빈 박스는 그대로였고, 나중에 호버 팝업이 잘리는 부작용만 남겼다 (→ TS-008).
  증상을 가리는 CSS와 원인을 없애는 수정은 다른 문제였다.

### 해결

카드 하나에 들어갈 여는 태그·내용·닫는 태그를 파이썬에서 **하나의 문자열로 조립한
뒤 `st.markdown`을 단 한 번만** 호출하도록 바꿨다.
`st.expander`나 `st.button`처럼 실제 위젯이 안에 들어가야 하는 구간(구단별 제시가,
핵심 요소 분석)은 애초에 한 div로 감쌀 수 없으므로, 억지로 감싸는 대신 감싸던
div 자체를 걷어내고 구획 제목만 따로 스타일링해 붙였다.

### 검증

홈 화면, 타자 찾기, 투수 찾기, 미래 FA 예정 선수 카드까지 브라우저에서 스크린샷으로
하나씩 확인. 빈 박스 없이 전부 정상 렌더링됐다.

### 추후 관리

`unsafe_allow_html`로 컨테이너를 그릴 때는 **여는 태그와 닫는 태그가 같은
`st.markdown` 호출 안에 있어야 한다**는 것을 규칙으로 둔다.
위젯을 품어야 하는 구간은 감싸지 말고 제목만 스타일링한다.

### 배운 점

이런 버그는 파이썬이 오류 없이 돌고 콘솔에도 안 남기 때문에 **코드만 읽어서는
절대 못 잡는다.** 실제로 브라우저에 띄워서 눈으로 봐야 발견된다.

---

## TS-006 — 카드 HTML이 들여쓰기 때문에 코드블록으로 그대로 노출됨

- **날짜**: 2026-08-03
- **분류**: UI

### 증상

TS-005를 고치던 도중, 홈 화면에서 여러 선수 카드 조각을 리스트로 모아 `join`한 뒤
바깥 문자열에 끼워 넣어 렌더링한 부분만 카드 UI 대신 **회색 배경 + 고정폭 글꼴로
`<div class=...>` 같은 태그 문자열이 글자 그대로** 노출됐다.

### 재현 조건

반복문 안에서 여러 줄 f-string으로 카드 조각을 만들고, 그걸 이어붙여 바깥 f-string의
치환 자리에 넣는 경우.

### 원인

- **표면 원인**: 마크다운이 해당 구간을 코드블록으로 해석해 HTML로 파싱하지 않았다.
- **근본 원인 — 확인함**: 반복문 안에서 만든 조각들은 **파이썬 소스의 들여쓰기 위치
  때문에** 앞에 공백이 여러 칸 붙어 있었다. 이어붙여 끼워 넣자 최종 문자열에서
  그 줄들이 4칸 이상 들여써진 채로 남았고, 마크다운은 줄 앞 공백이 4칸 이상이면
  그 구간을 코드블록으로 본다.

### 해결

여러 줄에 걸쳐 들여써져 있던 f-string 대신 **줄바꿈·들여쓰기 없는 한 줄짜리
f-string**으로 각 카드 조각을 다시 작성했다.

### 검증

화면을 확대해 태그 문자열이 코드처럼 표시되던 것을 확인 → 한 줄 문자열로 바꿔
합치자 곧바로 정상 HTML로 파싱됐다.

### 추후 관리

Streamlit·마크다운에 HTML을 주입할 때는 소스 코드의 보기 좋은 들여쓰기를 유지하려
하지 말고, **최종적으로 만들어질 문자열이 몇 칸 들여써지는지**를 따로 본다.

### 배운 점

같은 문자열도 소스 코드 어디서 만들어졌느냐에 따라 들여쓰기가 달라지고, 그대로
이어붙이면 결과물의 들여쓰기가 들쭉날쭉해진다.

---

## TS-007 — 소스를 고쳐도 브라우저 화면이 그대로 (재실행 배너가 CSS에 숨겨짐)

- **날짜**: 2026-08-03
- **분류**: 환경 / UI

### 증상

파일을 수정·저장하고 브라우저를 새로고침해도 예전 화면이 그대로 나왔다.
파일은 분명히 바뀌었는데 변경이 전혀 반영되지 않는 것처럼 보였다.

### 재현 조건

`streamlit run`을 옵션 없이 띄운 상태에서 소스 파일을 수정할 때마다.

### 원인

- **표면 원인**: Streamlit은 `server.runOnSave`가 기본으로 꺼져 있어 파일이 바뀌어도
  자동 재실행하지 않고, 화면 위쪽에 변경 감지 배너와 재실행 버튼만 띄운다.
- **근본 원인 — 확인함**: 이 프로젝트 전역 CSS에 `MainMenu`·`footer`·`header`를 전부
  숨기는 규칙이 있었고, **그 배너가 `header` 영역에 그려져서** 화면에서 아예 안 보였다.
  재실행 버튼이 없었던 게 아니라 숨겨져 있었을 뿐이다.

### 해결

`streamlit run` 실행 시 `server.runOnSave`를 켜서 파일 변경을 자동 반영하게 했다.
파이썬 임포트 캐시·`__pycache__`가 꼬였을 가능성까지 없애려고, 매번 프로세스를
완전히 종료하고 `__pycache__`를 지운 뒤 새로 띄우는 방식으로 확실히 했다.

### 검증

수정 → 저장 후 새로고침만으로 변경이 반영되는 것을 확인.

### 추후 관리

커스텀 CSS로 Streamlit 기본 UI를 숨기면 **그 요소가 담당하던 시스템 알림까지 같이
숨겨진다.** 기본 UI를 숨기는 규칙을 추가할 때 이걸 감안한다.

### 배운 점

화면이 안 바뀐다고 코드가 잘못 저장된 건 아닌지 의심하며 시간을 쓰기 전에,
서버를 아예 새로 띄워 그 가능성부터 지우는 편이 훨씬 빠르다.

---

## TS-008 — 컬럼 `overflow: hidden` 때문에 호버 팝업이 잘림

- **날짜**: 2026-08-03
- **분류**: UI

### 증상

홈 화면에서 다음 FA 주목 선수 카드에 마우스를 올리면 떠야 할 프로필 팝업이
카드가 속한 **컬럼 경계에서 그대로 잘려 나갔다.**

### 재현 조건

`st.columns`로 나눈 컬럼 안의 카드에 절대 위치 팝업을 붙였을 때 항상.

### 원인

- **표면 원인**: 컬럼 엘리먼트의 `overflow: hidden`.
- **근본 원인 — 확인함**: 이 규칙은 TS-005의 빈 div 박스가 있던 시절 화면이 지저분해
  보이는 걸 **가리려고 넣어둔 임시방편**이었다. 원인이 사라진 뒤에도 규칙만 남아,
  컬럼 밖으로 튀어나오도록 설계한 팝업의 발목을 잡았다.

### 해결

컬럼의 `overflow`를 `hidden` → `visible`로 바꿨다. TS-005를 근본적으로 고쳐 빈 박스
자체가 더 이상 안 생기므로, 이 규칙이 가리려던 문제는 이미 없어진 상태였다.

### 검증

`visible`로 바꾼 뒤 팝업이 카드 왼쪽으로 온전히 튀어나오는 것을 확인했고,
다른 화면에서 의도치 않은 가로 스크롤이나 레이아웃 깨짐도 생기지 않았다.

### 추후 관리

증상만 가리는 CSS를 넣을 때는 왜 넣었는지 주석으로 남긴다. 원인이 해결되면 같이 걷는다.

### 배운 점

임시방편 CSS는 진짜 원인이 없어진 뒤에도 남아서 **전혀 상관없는 다른 기능**의
발목을 잡는다. 이유를 알 수 없는 CSS 규칙은 지우기 전에 왜 넣었는지부터 추적한다.

---

## TS-009 — 선수 사진이 깨진 아이콘 (CDN이 Referer로 도용 차단)

- **날짜**: 2026-08-03
- **분류**: 외부 API / UI

> 이 건의 API 파라미터·응답 키·`pageSize` 부분은 **TS-001과 같은 원인**이고 거기서
> 자세히 다뤘다. 여기에는 TS-001에 없는 **Referer 차단** 부분만 남긴다.

### 증상

검색 화면의 원형 선수 사진과 홈 화면 FA 카드 호버 팝업 사진이 한 번도 뜬 적 없이
전부 이니셜 아바타로만 표시됐다. 에러는 전혀 안 났는데, 사진을 가져오는 함수가
**모든 예외를 조용히 삼키는 다단계 폴백**을 갖고 있어 실패해도 화면에 신호가
남지 않았기 때문이다. 원인을 하나씩 고칠 때마다 증상이 조금씩 형태를 바꿔가며 남았다.

### 재현 조건

이미지 URL을 정상적으로 얻은 뒤 `<img>` 태그로 그릴 때. 로컬호스트에서 항상.

### 원인

- **표면 원인**: 이미지 요청이 HTTP 403.
- **근본 원인 — 확인함**: 브라우저가 `<img>` 요청에 기본으로 붙이는 **로컬호스트
  주소의 `Referer` 헤더**를, 이미지를 호스팅하는 CDN이 도용 방지 목적으로 차단했다.
  URL 자체는 멀쩡했다.

### 시도했지만 안 된 것

- **개발 환경이 외부망을 막고 있다고 판단함** — 브라우저 자동화 도구로 네이버 스포츠를
  직접 열려다 **별개의 이유로** 차단당한 걸 네트워크 문제와 같은 것으로 착각했다.
  실제로는 요청이 서버까지 도달해 정상적으로 400 응답을 받고 있었으니, 애초에
  네트워크가 막혔다는 신호와 맞지 않는 상황이었다.
- **파라미터 이름만 고치고 끝났다고 판단함** → 응답 키가 안 맞아 목록이 계속 비었다.
- **파라미터 + 응답 키까지 고치고 끝났다고 판단함** → 특정 선수(전상현)가 계속
  안 나오는 걸 보고서야 `pageSize` 문제를 발견했다. (이 셋은 TS-001에서 정리)

### 검증

Referer를 바꿔가며 같은 이미지 URL을 요청해 차단 조건을 확정했다.

```
Referer: http://localhost:8501   ->  403
(Referer 없음)                    ->  200
Referer: https://sports.naver.com ->  200
```

`referrerpolicy="no-referrer"`를 적용한 뒤 Streamlit 앱을 재시작하고 브라우저에서
최정·이승현·전상현을 실제로 조회·호버해 **이니셜 아바타 대신 진짜 프로필 사진**이
뜨는 것을 눈으로 확인했다.

### 해결

사진을 그리는 `<img>` 태그 **두 곳 모두**에 `referrerpolicy="no-referrer"`를 지정해
브라우저가 `Referer` 헤더를 아예 안 보내도록 만들었다.

### 추후 관리

- 사진 함수의 다단계 폴백이 예외를 통째로 삼킨다. 실패 시 최소한 로그는 남기도록
  두는 편이 낫다 — 이 건에서 원인 파악이 길어진 직접적인 이유였다.
- 외부 CDN 이미지를 직접 그리는 자리에는 `referrerpolicy`를 기본으로 붙인다.

### 배운 점

**400·404처럼 서버가 명확한 형식으로 응답하는 에러는 네트워크가 막혔다는 신호가
아니다.** 요청이 서버까지 도달했고 서버가 그 요청을 이해한 다음 거절했다는 신호다.
연결 자체가 막혔다면 타임아웃이나 연결 거부로 나타나지 깔끔한 JSON 에러로
나타나지 않는다. 이 구분을 먼저 하지 않으면 엉뚱하게 환경 탓을 하다 원인 발견이 늦어진다.

이미지가 안 뜨는 문제는 **URL이 틀렸을 때만이 아니라 브라우저가 자동으로 붙이는
헤더 때문에도** 생긴다.

---

## TS-010 — 국가대표 명단을 대회 문서에서 읽어 아시안게임은 0명, 올림픽은 과다 집계

- **날짜**: 2026-08-17
- **분류**: 데이터 / 외부 API

### 증상

`star_score`를 만드는 `national_team` 값이 선수마다 들쭉날쭉했다. 김형준·나균안처럼
아시안게임 대표 경력이 분명히 있는 선수가 0으로 남아 있었다.
`scripts/collect_star_features.py`를 돌려 대회별 인원을 찍어 보니 이렇게 나왔다.

```
2026 WBC           2명   2024 프리미어12     1명
2022 아시안게임      5명   2018 아시안게임      5명
2014 아시안게임      5명   2009 WBC           3명
2021 올림픽         31명   2019 프리미어12    32명   2015 프리미어12   31명
```

아시안게임 3개 대회는 5명, 프리미어12·WBC 일부는 1~3명. 반대로 올림픽은 31명인데
**도쿄 올림픽 야구 엔트리는 24명**이다. 적게 나온 쪽과 많이 나온 쪽이 같이 있었다.

### 재현 조건

`NATIONAL_TEAM_PAGES`에 적힌 16개 대회 문서를 훑을 때마다 항상. 대회 문서 구조에만
의존하므로 매번 같은 결과가 나온다.

### 원인

- **표면 원인**: 대회 문서에서 명단을 못 찾거나, 명단이 아닌 것을 명단으로 읽었다.
- **근본 원인 (적게 나온 쪽) — 확인함**: 한국어 위키백과의 `2018년 아시안 게임 야구`
  같은 문서에는 **로스터가 아예 없다.** 경기 결과와 메달 표만 있는 문서다.
  5명은 명단이 아니라 문서 곳곳의 인명 링크가 잡힌 것이었다. `{{본문|...}}` 포인터를
  따라가는 경로도 있지만, 2026 WBC는 `선수 명단` 구획보다 `예선` 구획이 문서에서
  먼저 나와 **엉뚱한 문서(예선)로 따라갔고**, 정작 가리키는 `2026년 월드 베이스볼
  클래식 선수 명단`은 빨간 링크(문서 없음)였다.
- **근본 원인 (많이 나온 쪽) — 확인함**: 올림픽·프리미어12 문서는 명단 구획에
  **감독·코치가 같은 표에 들어 있다.** 걸러내는 규칙이 '감독'·'코치'라는 낱말이
  같은 줄에 있을 때만 동작해서, 별도 줄로 적힌 스태프가 선수로 딸려 들어갔다.

### 시도했지만 안 된 것

- **처음 세운 가설: "아시안게임 명단 문서가 다른 이름으로 있을 것"** — 틀렸다.
  `2022년 아시안 게임 야구 대한민국 선수 명단`, `틀:...` 등으로 찾아봤지만 전부 없다.
  한국어 위키백과에 아시안게임 야구 로스터 문서는 존재하지 않는다.
- **`{{연도}}년 아시안 게임 대한민국 선수단` 문서의 메달 표에서 뽑기** — 2014는
  24명으로 맞게 나왔지만, 2018은 16명만 나왔고 **2022는 메달리스트 구획이 통째로
  비어 있어** 아예 실패했다. 2018이 모자란 것은 `{{CompactTable}}` 칸 값을
  `[^\n|]+`로 잘라 읽은 탓이다. `[[이재원 (1988년)|이재원]]`처럼 파이프가 들어간
  이름이 중간에 끊겨 8명이 걸러졌다. 게다가 2014는 쉼표로 나열된 평문, 2018은
  `{{CompactTable}}`로 표 모양이 서로 달라 파서를 대회마다 만들어야 하는 구조였다.
- **영어 위키백과 로스터 문서 + langlinks로 한국어 이름 얻기** — 되긴 했다.
  `2022 아시안게임` 24명 중 18명, `2024 프리미어12` 28명 중 18명.
  하지만 **영어 위키에 문서가 없는 선수는 링크가 안 걸려 있어 통째로 빠진다.**
  김동헌·김지찬·최지민·윤동희가 그렇게 빠졌고, 로마자 표기를 한글로 되돌릴 방법이
  없어 복구할 수 없었다. `2026 WBC`는 표 형식이 달라 아예 파싱되지 않았다.

### 해결

문서가 아니라 **`분류:{연도}년 ... 참가 선수`** 에서 읽도록 바꿨다.
분류는 대회마다 이름 규칙이 같고, 선수 문서 쪽에 붙어 있어 대회 문서 구조와 무관하다.

```python
NATIONAL_TEAM_CATEGORIES = [
    ("2022 아시안게임", "분류:2022년 아시안 게임 야구 참가 선수"),
    ...
]
KOREAN_PLAYER_CATEGORY = "분류:대한민국의 야구 선수"
```

분류에는 참가국 선수가 전부 들어 있어(2009 WBC는 148명) `분류:대한민국의 야구 선수`가
붙은 사람만 남긴다. APBC 2017·2023만 참가 선수 분류가 없어 종전 문서 경로를 유지했다.

작업 중 걸린 것 두 가지:

- 한국 선수 필터가 처음에 절반을 떨어뜨렸다. `prop=categories`의 `cllimit`이
  **문서별이 아니라 응답 전체**에 걸려서, 50개를 한 번에 물으면 뒤쪽 문서의 분류가
  잘려서 온다. 10개씩 끊고 `continue`도 따라가도록 고쳤다.
- `ko.wikipedia.org` 읽기 타임아웃이 간헐적으로 났다. 세션을 재사용하고 3회까지
  재시도하게 했다.

### 검증

```
2026 WBC          22명 (44명 중)     2024 프리미어12    28명 (35명 중)
2023 WBC          30명 (152명 중)    2022 아시안게임     24명 (26명 중)
2021 올림픽         24명 (77명 중)     2019 프리미어12    28명 (67명 중)
2018 아시안게임      24명 (25명 중)     2017 WBC          31명 (174명 중)
2015 프리미어12      28명 (81명 중)     2014 아시안게임     24명 (28명 중)
2013 WBC          27명 (144명 중)    2009 WBC          28명 (148명 중)
2008 올림픽         24명 (73명 중)     2006 WBC          29명 (150명 중)
```

아시안게임 3개 대회가 모두 **정확히 24명** — 아시안게임 야구 엔트리 수와 일치한다.
올림픽도 31명에서 실제 엔트리인 24명으로 내려왔다.
국가대표 기록 287건 → 410건, 등재 선수 174명 → 201명.
김형준 0→3회, 나균안 0→1회, 노시환 1→3회, 박영현 0→2회.

`pytest` 25개 통과, 전 모듈 import 확인.

### 성능 — 타자는 올랐고 투수는 내렸다

| 대상 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 타자 | R² 0.541 / RMSE 4.96억 | **R² 0.618 / RMSE 4.53억** |
| 투수 | R² 0.677 / RMSE 2.64억 | **R² 0.645 / RMSE 2.77억** |

타자는 블렌드 조합도 XGBoost+Ridge에서 LightGBM+Ridge로 바뀌었다.
투수가 내려간 것은 표본이 46명뿐이라 피처 하나에도 수치가 크게 흔들리기 때문으로
보인다. **확인한 것은 여기까지고, 어떤 선수 때문에 내려갔는지는 따로 보지 않았다.**
TS-004와 같은 이유로 되돌리지 않았다 — 데이터가 사실에 맞아진 쪽을 택했다.

### 추후 관리

- **2023 APBC는 여전히 18명이다.** 실제 엔트리는 26명. 참가 선수 분류가 없고,
  문서가 가리키는 `2023년 아시아 프로야구 챔피언십 선수 명단`은 빨간 링크이며
  영어 위키백과에도 해당 대회 문서 자체가 없다. 남은 공백은 이것 하나다.
- WBC는 엔트리가 30명인데 2026년 22명, 2013년 27명, 2006년 29명이 나온다.
  한국어 위키백과에 문서가 없는 선수는 분류에도 안 잡히기 때문이다. 다만 그런 선수는
  우리 FA 표본에도 거의 없어 영향이 작다.
- 새 대회가 열리면 `NATIONAL_TEAM_CATEGORIES`에 분류 한 줄만 추가하면 된다.
  분류가 생기기 전이라면 비어서 나오므로, 실행 로그의 `<- 확인 필요` 표시를 본다.

### 배운 점

**같은 사실을 담은 소스라도 어느 쪽에 붙어 있느냐가 파싱 난이도를 정한다.**
대회 문서는 대회마다 사람이 다르게 써서 구조가 제각각이지만, 분류는 선수 문서 쪽에
붙는 표준화된 메타데이터라 대회가 바뀌어도 읽는 방법이 같다. 문서 파서를 대회 수만큼
늘리기 전에, 같은 정보가 더 정형화된 자리에 있지 않은지 먼저 봐야 했다.

**너무 적게 나온 것만큼 너무 많이 나온 것도 신호다.** 아시안게임 0명은 눈에 띄어서
바로 문제로 인식했지만, 올림픽 31명은 '충분히 많으니 됐다'고 넘어갔었다. 엔트리 수라는
바깥 기준과 대보고 나서야 그쪽도 틀렸다는 걸 알았다. 수집 결과는 많고 적음이 아니라
**실제로 몇 명이어야 하는지**와 대봐야 한다.
